class A {
    a : Int;
    suma (a : Int, b : Int) : Int {
        a + b
    };
    b : Int <- 8;
}

class B inherits A {
    a : A;
    f (d : Int, a : A) : Void {{
        let f : Int <- 8, c : A <- new A in
            c.suma(5 ,f);
        c;
	}};
}
